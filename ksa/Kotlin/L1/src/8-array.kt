fun main() {
    var name = arrayOf("a","b","c")  // make array
//    if you want change any think  must be for the printer not after
    name[1] = "salawe"
    println(name[1]) //print one element from array
    println(name.size)// rang array
    println(name.indexOf("A")) // number index
    println(name.contains("E")) //false
    println(name.last()) //last one in the array
    println(name.sorted()) //print all element


}